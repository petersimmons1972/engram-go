---
name: Session 2026-04-12 — Supabase + K8s Infra
description: Supabase TrueNAS deployment complete + 4 K8s infra fixes + predictive monitoring added
type: project
originSessionId: cee1f2e3-3a55-47af-a2e6-202bb29ee1b4
---
## Supabase on TrueNAS SCALE 25.10 — COMPLETE

All 13 services healthy. Root causes fixed:
1. **supabase-auth/storage/pooler restarting** → Base64 POSTGRES_PASSWORD was not URL-encoded in `fetch-secrets.sh`. Go URL parser treated `/` as path separator. Fix: `PG_PASS_ENCODED=$(python3 -c "import urllib.parse, sys; print(urllib.parse.quote(sys.argv[1], safe=''))" "${POSTGRES_PASSWORD}")` then use `${PG_PASS_ENCODED}` in all connection URLs.
2. **realtime-dev unhealthy (403)** → upstream `docker-compose.yml` has hardcoded `placeholder` JWT. Fix: replaced with `${ANON_KEY}` in CMD-SHELL healthcheck.
3. **Studio not accessible** → missing `ports: - 3000:3000` on studio service.

Key files:
- `/home/psimmons/projects/supabase-homelab/fetch-secrets.sh`
- `/home/psimmons/projects/supabase-homelab/docker-compose.truenas.yml`

## K8s Infrastructure — COMPLETE

1. **CoreDNS split-brain** → `coredns-custom` ConfigMap created in `kube-system` forwarding `petersimmons.com` to Unifi 192.168.0.1. Pi-hole does NOT have internal records.
2. **content-cache missing schema** → init container ran but schema wasn't applied. Fixed via `kubectl cp` + `psql -f schema.sql`.
3. **proxmox-monitor CrashLoopBackOff (117 days)** → DB StatefulSet was already 0 replicas (archived). Scaled Deployment to 0 as well.
4. **Stale dossier-scan Error pods** → deleted (3 pods from pre-schema runs).

## Predictive Monitoring Added

`~/bin/health-check.sh` now checks:
- `coredns-custom` ConfigMap presence (load-bearing for internal DNS)
- Deployments with `DB_HOST` pointing to 0-replica StatefulSets (archived DB crash detection)

5 wisdom patterns stored to Engram (tags: postgres, url-encoding, coredns, split-brain, init-containers, archive, supabase, realtime).

**Why:** Engram had schema migration error (migration 009_backfill_content_hash.sql bytea syntax error) — file fallback used.

## Open Items
- **Stage 6 Clearwatch fixes** — 7 pending from prior session, NEXT PRIORITY
- **proxmox-monitor full decommission** — Deployment/StatefulSet/ConfigMap/Secret/ServiceMonitor still deployed; file GitHub Issue

# Centralized Engram endpoint config — sourced by all engram hooks.
# Switched from localhost tunnel (http://127.0.0.1:8788) to stable ingress
# on 2026-05-29 after confirming: /health 200, /sse 200 with Bearer token.
# Port-forward service (engram-portforward.service) is kept as fallback.
ENGRAM_BASE_URL="https://engram.petersimmons.com"
